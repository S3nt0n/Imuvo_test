<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sco.imuvo">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ImuvoTheme.">
        <activity android:name=".Activities.LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Menu"/>
        <activity android:name=".Activities.CreateUserActivity" />
        <activity android:name=".Activities.vocabReadingSelection" />
        <activity android:name=".Activities.readVocabs" />
        <activity android:name=".Activities.VocabList" />
    </application>
</manifest>